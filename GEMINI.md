# GEMINI.md

Use `AGENTS.md` as the source of truth for this repository.

Key repo constraints:

- This is the iOS SwiftPM SDK for API trace capture, not an app.
- Products: `APITraceCore`, `APITraceDebug`, `APITraceNoop`.
- Shared behavior changes should be reviewed alongside the Android counterpart.
- Keep public docs in sync with code changes.
- Validate with `swift test`.
